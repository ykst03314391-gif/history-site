# トピック（topic）スキーマ設計 v0.1 ― 最新情報（B）の軽量フォーマット

コンセプトの **B「仕入れる（最新情報）」** を担う軽量コンテンツ。
新説・お出かけ（展示/イベント）・ゲーム情報を「**情報のお知らせ**」として軽く扱う（踏み込んだ考察はしない）。深掘りの考察は記事（A）へ、詳細は DB（人物・史跡）へ流す。

> 位置づけ：B は**入口・フック**であって看板ではない（看板は常に A＝考察・視点）。
> 詳細は README「看板は『視点』、最新情報は『呼び込み』」を参照。

## 記事（article）との違い

| | article（考察・A） | topic（最新情報・B） |
|---|---|---|
| 目的 | 深掘り・視点（看板） | お知らせ・鮮度（呼び込み） |
| 本文 | `data/articles/<id>.md`（長文） | **本文ファイルなし**。`index.json` のフィールドだけで完結 |
| 中身 | 切り口・主張＝**考察** | **お知らせ**（何が新しいか）。考察はしない＝Aの仕事 |
| 手間 | 管理人が時間をかけて書く労作 | 定期・不定期に**自動的に蓄積**する軽いもの |
| 動線 | それ自体が目的地 | **A（考察）・DB へ誘導するのが役目** |

## 設計方針（＝差別化をスキーマで守る）

1. **B はお知らせにとどめる**。踏み込んだ考察・見立てはここに置かない（それは A の仕事）。`take` は任意の軽い「ひとこと」にすぎず、本格的な考察は必ず関連考察記事（A）で展開する。
2. **一次情報源 `source` を必ず持つ**（公式サイト・ニュース等）。お知らせは出どころを示す。
3. **A への動線を推奨**：`relatedArticles`（深掘り）／`relatedPeople`（DB）で必ずどこかへ流す。考察を書いたら `relatedArticles` でその記事へつなぐ。
4. **軽い・蓄積型**：本文 md は作らない。定期・不定期の収集をすばやくお知らせ化し、貯めていけるように。

## トップレベル構造（`data/topics/index.json` の `topics[]`）

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | string | 一意なID（例：`tedorigawa-reassessment`） |
| `title` | string | 速報の見出し |
| `stream` | string | `新説` / `お出かけ` / `ゲーム`（B の3ストリーム） |
| `date` | string | 掲載・収集日（YYYY-MM-DD） |
| `summary` | string | **お知らせ本文**：何が起きた／何が新しいか（事実ベース。一覧カードにも出る短め） |
| `body` | string | 詳しい紹介文（任意。詳細ページで `summary` の下に表示）。事実の紹介にとどめ考察はしない |
| `images` | Image[] | 画像 `{ "src", "caption"?, "credit"?, "license"? }`（任意）。**権利に注意**：美術館・主催の展示写真やチラシは著作権があるため転載しない。パブリックドメイン画像（例：Wikimedia Commons の肖像画）を `credit`/`license` 明記で使うか、自分で撮影・作成したものを使う。参考イメージは caption にその旨を書く |
| `highlights` | string[] | 見どころ・扱う対象の箇条書き（任意。お出かけ向け） |
| `info` | Info[] | 追加情報の行 `{ "label": string, "value": string }`（任意。開館時間・休館日・観覧料・主催・問合先など）。会期は `period`、会場は `place` を使う |
| `events` | Event[] | 関連イベント `{ "name": string, "schedule"?: string, "note"?: string }`（任意。講演会・見学会など） |
| `take` | string | **ひとこと（任意）**：軽い補足のみ。本格的な考察は書かない（→ A）。空でよい |

### 新説ストリーム専用フィールド（`stream: "新説"` のとき詳細を別レイアウトで表示）

新説の詳細ページは、お出かけ・ゲームとは別の4部構成で表示する（`topic.js` が `stream === "新説"` で分岐）。

| フィールド | 型 | 説明 |
|---|---|---|
| `sourceOverview` | string | **① 情報元の概要**：誰が・どこで・どんな形で出した説か |
| `detail` | string | **② 内容の詳細**：新説の中身（`detail` が無ければ `body` で代用） |
| `conventional` | string | **③ 従来（通説）** の見方（対比の左） |
| `revision` | string | **③ 新説** の見方（対比の右）。`conventional` と並べて「従来と何が違うか」を表示 |
| `comment` | string | **④ 管理人のコメント**：短い意見（新説だけは意見を添える。深い考察は関連考察記事＝Aへ） |

> 新説は「事実の報告＋短いコメント」まで。お出かけ・ゲームは純粋なお知らせ（`take` は軽い一言のみ）。
| `source` | Source | 一次情報源 `{ "name": string, "url": string }` |
| `relatedArticles` | string[] | 深掘り考察記事の `id`（→ A への動線） |
| `relatedPeople` | string[] | 関連人物の `person_id`（→ DB への動線） |
| `relatedBattles` | string[] | 関連合戦の `battle_id`（任意） |
| `period` | Period | お出かけの会期 `{ "from": string, "to": string }`（任意） |
| `place` | Place | お出かけの場所 `{ "name", "prefecture", "city", "address"?, "lat"?, "lng"? }`（任意）。`address` は会場の下に表示。`lat`/`lng`（緯度・経度）があれば詳細ページに**地図（OpenStreetMap 埋め込み、APIキー不要）**と Google マップへのリンクを表示。座標は Nominatim 等で会場名から取得できる |
| `platform` | string | ゲームのプラットフォーム（任意） |
| `tags` | string[] | タグ（任意） |
| `buy` | Buy[] | 購入・予約リンク `{ "shop": string, "url": string, "note"?: string }`（任意）。**アフィリエイト**（Amazonアソシエイト等）。詳細ページに「購入・予約」ボタン＋開示文を表示 |
| `draft` | boolean | 下書き（true は一覧に出さない、任意） |

### アフィリエイト（`buy`）の運用ルール

- リンクは自分の**アソシエイトタグ**付きにする（例：`...&tag=xxxx-22`）。登録前は仮タグのままにしておき、承認後に差し替える。
- **開示は必須**。詳細ページのボタン下に「※アフィリエイトを含みます。Amazonのアソシエイトとして、当サイトは適格販売により収入を得ています。」を自動表示する。
- リンクの `rel` は `sponsored nofollow noopener`（有償リンクの正しい表記）。
- 発売前でも**予約ページ**があれば貼れる。書籍・図録なども同じ仕組みで `references`/`buy` に使える。

## ストリーム別の使い分け

- **新説**：話題の新説・新研究・新刊の**告知**。評価・考察は書かず、`relatedArticles`/`relatedPeople` で深掘り（A）へ流す。
- **お出かけ**：博物館の特別展・戦国イベント等の**会期もの**の告知。`period`（会期）と `place`（場所）を入れ、`place.prefecture` で将来「地域から探す」と連携。`relatedPeople` でその人物の展示だと示す。
- **ゲーム**：信長の野望など新作・大型アップデートの告知。`platform` を入れる。実像に照らした比較・評価は書かず、A の企画（"信長の野望、実は間違ってる？"）へ接続する。

## 表示仕様（一覧 → 詳細）

- **一覧**：`topics.html` … 新着順フィード。stream 別に絞り込み。`draft` は除外。トップ `index.html` の「最新トピック」セクションも同じカードを表示する（看板は考察＝A、トピックはその下）。
- **一覧カード**：stream バッジ・日付・タイトル・`summary`（お知らせ）・お出かけは `period`/`place`・`take` があれば軽い「ひとこと」。**カード全体がクリックで詳細ページへ遷移するリンク**（出どころ・関連リンクはカードに置かず詳細へ）。
- **詳細**：`topic.html?id=<id>` … 一覧の内容に加え、`source`（出どころ）への外部リンク、`relatedArticles`/`relatedPeople` への動線、`tags` を表示。**※ 詳細ページの中身は今後さらに拡張する（次の指示で仕様を詰める）。**

## 今後の拡張

- お出かけの `place.prefecture` を「地域から探す」に統合（今そこで何が見られるか）。
- 収集の定期運用（スケジュール実行で候補を集め、`take` を付けて公開）。
